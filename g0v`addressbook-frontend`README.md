addressbook-frontend
====================

`prerequisites` :
  * node.js
  * bower

`local dev` :

  1. `bower install`
  2. `npm install`
  3. `npm install -g grunt-cli`
  4. run static server for development
    `grunt watch`
